import { TranslateAlertPipe } from './translate-alert-pipe';

describe('TranslateAlertPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateAlertPipe();
    expect(pipe).toBeTruthy();
  });
});
